<!DOCTYPE html>
<html>

  <head>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
    <h1>Welcome to the Warehouse Manager!</h1>
  </head>
  
  <body>
    <table> 
      <h3>Viewing all Products!
      <th>ID    </th><th>Cat ID</th><th>Loc ID</th>
      <% @products.each { |x| %>
      <tr>
        <td><%= x.id  %></td>
        <td><%= x.category_id %></td>
        <td><%= x.location_id %> 
      </tr> <% } %>
    </table>
    <table> 
      <h3>Viewing all Categories!</h3>
      <th>ID</th><th>Name</th><th>Cost</th><th>Description</th>
      <% @categories.each { |x| %>
      <tr>
        <td><%= x.id  %></td>
        <td><%= x.name %></td>
        <td><%= x.cost %></td>
        <td><%= x.description %></td>
       </tr> <% } %>
    </table>
    <table> 
      <h3>Viewing all Locations!</h3>
      <th>ID</th><th>Name</th>
      <% @locations.each { |x| %>

      <tr>
        <td><%= x.id  %></td>
        <td><%= x.name %></td> 
      </tr> <% } %>
    </table>
    
    <div class="yielded"><%= yield %></div>
  </body>
  <nav>
     <ul>
       <li><a href="/">Return to Homepage</a></li>
     </ul>
  </nav>
  
</html>